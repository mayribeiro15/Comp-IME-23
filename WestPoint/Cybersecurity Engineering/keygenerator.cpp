#include <bits/stdc++.h>
using namespace std;

int main() {
  int p,q,e,n,phi;
  cout << "different primes p,q: ";
  cin >> p >> q;
  
  n = p*q;
  phi=(p-1)*(q-1);
  
  cout << "relatively prime less than" >> phi >> ": ";
  cin << e;
  
  cout << "PU = {e,n} = " << endl
  cout << "PR = {d,n} = " << endl

  while(1){
    cout << "encrypt:"
  }
}